
# Push code to git
git add .
git commit -m "add"
git push


# Merge UAT to main
git checkout main
git fetch origin
git pull origin main
git merge UAT

# Push merged main

git push origin main

# If there are conflicts, Git will mark the files; you need to manually resolve them, then:

git add <resolved-files>
git commit